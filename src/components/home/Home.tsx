import React, { memo } from 'react';

import './Home.scss';

const Home = (): JSX.Element => {
    return (
        <div id='Home'>
            Home
        </div>
    )
};

export default memo(Home);